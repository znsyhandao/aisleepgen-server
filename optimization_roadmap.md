# AISleepGen 顶尖改造方案

> 2026-06-27 至尊宝 + 小甜甜
> 不做功能堆砌，做用户感知到的质变

---

## 读完代码后的判断

AISleepGen 代码深度远超我的预期。列几个让我意外的：

- **已经有的**：8专家会诊、循证证据匹配、情绪识别、面容疲劳分析、自学习校准、偏好引擎、拓扑动力学、跨日趋势分析、多轮对话记忆
- **真正缺的不是功能，是"表达力"** — 所有模块都在输出数据，但输出给deepsek的数据没有被打磨成"有洞察的话"
- **最大的浪费**：自学习校准（SelfLearn）、偏好引擎（PreferenceEngine）、趋势分析（_trend_analysis）都在默默算，算出来的东西没有真正让用户感知到

**核心问题一句话：AISleepGen 已经是一个很深的系统，但用户的感知停留在很浅的层面。**

原因是：所有深度分析的结果都被压扁成了几个数字塞进prompt，deepseek拿到的是`score=0.7, findings=[...]`这样的结构化数据，它输出的回复就只能是"你的ISI分数是18，属于中度失眠"这种平铺直叙。

---

## 真正的改造思路

**不是改架构，不是加功能。是把已有的深度，翻译成用户能感知的深度。**

### 改造1：专家输出 → 会诊意见书（3天）

**现在**：每个专家`analyze()`返回 `{"score":0.7, "findings":["入睡困难+焦虑评分偏高"], "risk_flags":["抑郁风险"]}`

**改造后**：每个专家返回同样数据，但**新增一个 `narrative` 字段** — 一段用deepseek生成的、像真实医生写的那种"会诊意见"。

```
{
  "score": 0.7,
  "findings": [...],
  "narrative": "根据用户提供的睡眠数据（入睡潜伏期45分钟、夜间醒来2次、自评压力6/10），
               模拟PHQ-9评分约15/27，提示中度抑郁倾向。用户描述'入睡困难'与焦虑评
               分偏高形成一致性表现，符合焦虑性失眠表型特征。建议：...（引用证据）"
}
```

**最终汇总时**，deepseek拿到的是10段有血有肉的专业意见，不是10个干巴巴的数字。这就是回答质量的质变。

**为什么这不是"加一次deepseek调用"这么简单？**
- 需要设计每个专家的"专业身份"和"分析视角"
- 每个专家的narrative需要用该领域的术语体系
- 需要控制token消耗（每个专家 ~150 tokens，10个 = 1500 tokens）

---

### 改造2：趋势分析 → 个人睡眠报告（2天）

**现在**：`_trend_analysis()` 只算最早和最新的评分差值，输出"提升10分"或"下降10分"

**改造后**：做真正的多维趋势分析：
- 入睡时间趋势（逐日 → 发现"周六晚熬夜"模式）
- 睡眠时长滑动平均（7天窗口）
- 心率变异性趋势
- 用户情绪变化和睡眠质量的关联

**展示方式**：每7天输出一段"个人周报"风格的总结：
```
本周你的平均入睡时间是23:42，比上周晚了12分钟。
周三和周五你睡了不到6小时，但周六补了9小时——典型的"社交时差"模式。
你的心率变异性在下降，提示累积疲劳。
```

**已有数据**：user_profile.json的history已经有每天的失眠数据（入睡时间、醒来时间、评分、情绪）。不需要额外存储。

---

### 改造3：自学习校准 → 用户可见的进化（1天）

**现在**：`self_learn.py` 默默收集反馈、调整pain_penalty等参数，用户完全不知道系统在学他。

**改造后**：在回复中偶尔出现"学习痕迹"：
- "根据你对上周建议的反馈，我调整了..."
- "我注意到你更喜欢白噪音而非冥想，今晚推荐了一个新的..."
- 使用超过N次后，显示"小甜甜已陪你X天"

用户感知到AI在进化，不是固定不变的机器人。

---

### 改造4：拓扑动力学 → 睡眠韧性指数 + 趋势图（1天）

**同v2方案，但更具体：**
- 每日计算phi/psi/h → 归一化为0-100分
- 存到user_profile里（已有history结构支持）
- 每周生成韧性指数趋势句子

---

## 执行顺序

```
Day 1-3: 改造1（专家会诊意见）← 核心，最早见效
Day 4-5: 改造2（多维趋势分析）
Day 6:   改造3（自学习可见化）
Day 7:   改造4（韧性指数展示）
```

## 不改的

- 架构（WorldModelEngine完整保留）
- 调用链路（comprehensive_analysis → deepsek 不变）
- 数据存储（全复用现有字段）
- LLM（deepseek-chat）

## 预期效果对比

| 维度 | 现在 | 改造后 |
|------|------|--------|
| 用户打开看到 | "你昨晚睡了6.5小时，质量一般" | "昨晚你睡了6.5小时，但深睡比例偏低。结合你上周的数据，你的入睡时间在向凌晨偏移——可能和最近的工作压力有关。" |
| 用了3天后 | 同上 | "和3天前相比，你的HRV有改善，但入睡仍然困难。上周建议的深呼吸你试了吗？" |
| 用了2周后 | 同上 | "这两周你的睡眠模式有周期性变化——工作日短、周末补。你属于典型的社交时差，建议周末起床时间偏移不超过1小时。" |

**根本变化：每次回答都让用户觉得"这个AI在认真看我"，而不是"这个AI在套模板"。**
